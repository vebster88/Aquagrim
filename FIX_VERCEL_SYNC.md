# Исправление проблемы с автоматическими деплоями Vercel

## Проблема
После изменения видимости репозитория (приватный → публичный) автоматические деплои перестали запускаться при push в GitHub.

**Важно:** Vercel использует GitHub App для получения событий, а не вебхуки. Если в других проектах нет вебхуков, но деплои работают - это нормально.

## Решение

### Шаг 1: Проверка GitHub App (ГЛАВНОЕ!)

Vercel использует GitHub App для автоматических деплоев. Проверьте:

1. GitHub → **Settings** → **Applications** → **Installed GitHub Apps**
2. Найдите **Vercel**
3. Нажмите на него и проверьте:
   - ✅ **Repository access** - должен быть доступ к репозиторию `Aquagrim`
   - ✅ Если выбрано "Only select repositories" - убедитесь, что `Aquagrim` в списке
   - ✅ Если выбрано "All repositories" - должно работать

**Если репозитория нет в списке:**
- Нажмите **"Configure"** рядом с Vercel
- В разделе **"Repository access"** выберите:
  - **"All repositories"** (рекомендуется)
  - Или **"Only select repositories"** и добавьте `Aquagrim`
- Сохраните изменения

### Шаг 2: Проверка настроек автоматических деплоев в Vercel

1. Vercel Dashboard → ваш проект → **Settings** → **Git**
2. Проверьте:
   - ✅ **Production Branch** = `main` (или ваша основная ветка)
   - ✅ **Automatic deployments from Git** должен быть включен
   - ✅ **Deploy Pull Requests** (опционально)

**Если "Automatic deployments from Git" отключен:**
- Включите его
- Сохраните изменения

### Шаг 3: Переподключение репозитория (если Шаги 1-2 не помогли)

1. Откройте **Vercel Dashboard** → ваш проект
2. Перейдите в **Settings** → **Git**
3. Нажмите **"Disconnect"** рядом с подключенным репозиторием
4. Подтвердите отключение
5. Нажмите **"Connect Git Repository"**
6. Выберите **GitHub**
7. Выберите ваш репозиторий `vebster88/Aquagrim`
8. Нажмите **"Import"**

### Шаг 4: Переподключение репозитория (если предыдущие шаги не помогли)

1. В Vercel Dashboard → **Settings** → **Git**
2. Нажмите **"Disconnect"** рядом с подключенным репозиторием
3. Подтвердите отключение
4. Нажмите **"Connect Git Repository"**
5. Выберите **GitHub** → ваш репозиторий `vebster88/Aquagrim`
6. Нажмите **"Import"**

**При переподключении:**
- Vercel может запросить разрешения GitHub App
- Убедитесь, что предоставили доступ к репозиторию

### Шаг 5: Тестирование

После переподключения создайте тестовый коммит:

```bash
git commit --allow-empty -m "Test Vercel auto-deploy"
git push
```

**Ожидаемый результат:**
- В течение 1-2 минут в Vercel Dashboard должен появиться новый деплой
- В GitHub App события должны обрабатываться автоматически (без вебхуков)

## Важно: Vercel использует GitHub App, а не вебхуки

**Современные проекты Vercel используют GitHub App для автоматических деплоев:**
- ✅ Не требуют вебхуков в GitHub
- ✅ Работают через GitHub App API
- ✅ Более надежны и безопасны

**Если в других проектах нет вебхуков, но деплои работают - это нормально!**

Проблема скорее всего в:
1. **GitHub App не имеет доступа к репозиторию** (проверьте Шаг 1)
2. **Автоматические деплои отключены в Vercel** (проверьте Шаг 2)
3. **Production Branch настроен неправильно** (проверьте Шаг 2)

## Частые проблемы

### Проблема: Вебхук отправляется, но деплой не запускается

**Решение:**
1. Проверьте, что Production Branch в Vercel = `main`
2. Убедитесь, что push был в ветку `main`
3. Проверьте логи вебхука в GitHub

### Проблема: GitHub App не имеет доступа к репозиторию

**Решение:**
1. GitHub → **Settings** → **Applications** → **Installed GitHub Apps** → **Vercel**
2. Нажмите **"Configure"**
3. В разделе **"Repository access"** выберите:
   - **"All repositories"** (рекомендуется)
   - Или **"Only select repositories"** и добавьте `Aquagrim`
4. Сохраните изменения
5. Переподключите репозиторий в Vercel

### Проблема: "Repository not found" при переподключении

**Решение:**
1. Убедитесь, что репозиторий существует и доступен
2. Проверьте, что вы авторизованы в Vercel под правильным GitHub аккаунтом
3. Убедитесь, что репозиторий не в архиве

## После исправления

После настройки GitHub App и переподключения репозитория:
1. ✅ Автоматические деплои должны работать при push в `main`
2. ✅ GitHub App будет получать события от GitHub автоматически
3. ✅ Каждый push должен создавать новый деплой в Vercel
4. ✅ Вебхуки не требуются - Vercel использует GitHub App API

## Проверка успешности исправления

1. Сделайте тестовый push:
   ```bash
   git commit --allow-empty -m "Test auto-deploy after fix"
   git push
   ```

2. Проверьте Vercel Dashboard → Deployments
   - Должен появиться новый деплой в течение 1-2 минут

3. Проверьте GitHub → Settings → Webhooks → Recent Deliveries
   - Должен быть новый запрос с кодом 200

Если все работает - проблема решена! 🎉

