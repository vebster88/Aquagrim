# План дальнейших шагов

## ✅ Текущее состояние проекта

- [x] Структура проекта создана
- [x] Код написан и проверен
- [x] Git репозиторий инициализирован
- [x] Проект загружен в GitHub
- [ ] Зависимости не установлены
- [ ] Файл .env не создан
- [ ] Бот не настроен
- [ ] Vercel KV не подключен

## 📋 Пошаговый план действий

### Шаг 1: Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты:
- `telegraf` - для работы с Telegram Bot API
- `@vercel/kv` - для работы с Vercel KV
- `pdfkit` - для генерации PDF
- `typescript` и другие dev-зависимости

### Шаг 2: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Aquagrim Reporting Bot")
   - Введите username бота (например: "aquagrim_reporting_bot")
4. Скопируйте полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 3: Получение Telegram ID

Чтобы стать супер-админом:

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш Telegram ID (число, например: `123456789`)

### Шаг 4: Создание Vercel KV базы данных

#### Вариант A: Через Vercel Dashboard (рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com) и авторизуйтесь
2. Создайте новый проект (или используйте существующий)
3. Перейдите в раздел **Storage**
4. Нажмите **Create Database** → выберите **KV**
5. Создайте KV базу данных
6. Скопируйте:
   - `KV_REST_API_URL` (например: `https://your-kv.vercel-storage.com`)
   - `KV_REST_API_TOKEN` (длинная строка токена)

#### Вариант B: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Создайте KV базу (в директории проекта)
vercel kv create
```

### Шаг 5: Создание файла .env

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
KV_REST_API_URL=ваш_kv_rest_api_url
KV_REST_API_TOKEN=ваш_kv_rest_api_token
SUPERADMIN_IDS=ваш_telegram_id
PORT=3000
```

**Важно:** Файл `.env` уже добавлен в `.gitignore`, поэтому он не попадет в Git.

### Шаг 6: Тестирование локально

```bash
# Соберите проект
npm run build

# Запустите в режиме разработки
npm run dev
```

Бот будет работать в режиме polling (опрос Telegram API каждые несколько секунд).

**Проверка:**
1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Должно появиться главное меню с кнопками

### Шаг 7: Деплой на Vercel

#### 7.1. Подготовка

```bash
# Убедитесь, что Vercel CLI установлен
npm i -g vercel

# Войдите в аккаунт (если еще не вошли)
vercel login
```

#### 7.2. Деплой

```bash
# Деплой в продакшен
vercel --prod
```

Следуйте инструкциям:
- Выберите существующий проект или создайте новый
- Подтвердите настройки

#### 7.3. Настройка переменных окружения в Vercel

После деплоя:

1. Перейдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте все переменные из `.env`:
   - `BOT_TOKEN`
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`
   - `SUPERADMIN_IDS`

#### 7.4. Настройка вебхука Telegram

После деплоя получите URL вашего проекта (например: `https://aquagrim-bot.vercel.app`)

Настройте вебхук одним из способов:

**Способ 1: Через браузер**
```
https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://your-project.vercel.app/api/webhook
```

**Способ 2: Через curl (PowerShell)**
```powershell
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" -d "url=https://your-project.vercel.app/api/webhook"
```

**Способ 3: Через curl (Git Bash)**
```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-project.vercel.app/api/webhook"
```

Замените `<BOT_TOKEN>` на ваш реальный токен и `your-project.vercel.app` на ваш URL.

**Проверка вебхука:**
```
https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo
```

### Шаг 8: Проверка работы в продакшене

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте все функции:
   - Утреннее заполнение
   - Вечерний отчет
   - Редактирование
   - Админ-панель (если вы супер-админ)

## 🔧 Дополнительные настройки

### Настройка автоматического деплоя из GitHub

1. В Vercel Dashboard перейдите в **Settings** → **Git**
2. Подключите репозиторий GitHub
3. Включите **Automatic deployments**
4. Теперь каждый push в `main` будет автоматически деплоить проект

### Мониторинг и логи

- **Логи Vercel:** Dashboard → ваш проект → **Logs**
- **Логи бота:** Все действия логируются в Vercel KV (таблица Log)

## ⚠️ Важные замечания

1. **Безопасность:**
   - Никогда не коммитьте `.env` файл в Git
   - Храните токены в секретах Vercel
   - Не делитесь токенами публично

2. **Производительность:**
   - Vercel Functions имеют лимиты на время выполнения
   - Для больших нагрузок рассмотрите использование Vercel Pro

3. **Резервное копирование:**
   - Регулярно экспортируйте данные из Vercel KV
   - Настройте автоматические бэкапы

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что вебхук настроен правильно
4. Убедитесь, что Vercel KV активен и доступен

## 🎯 Чеклист готовности

- [ ] Зависимости установлены (`npm install`)
- [ ] Telegram бот создан и токен получен
- [ ] Telegram ID получен
- [ ] Vercel KV создан и credentials получены
- [ ] Файл `.env` создан и заполнен
- [ ] Локальный запуск работает (`npm run dev`)
- [ ] Проект задеплоен на Vercel
- [ ] Переменные окружения настроены в Vercel
- [ ] Вебхук Telegram настроен
- [ ] Бот работает в продакшене

После выполнения всех пунктов бот готов к использованию! 🚀



