# Telegram Bot для отчетности аквагрима

Telegram-бот для автоматизации сбора отчетности и подсчета выручки по площадкам аквагрима.

## 🎯 Цель проекта

Автоматизация процесса сбора отчетности:
- Утреннее заполнение площадок
- Вечерние отчеты по площадкам
- Автоматический расчет финансовых показателей
- Генерация PDF-отчетов
- Админ-панель для управления

## 🏗️ Архитектура

Проект построен на принципах разделения ответственности:

- **`src/bot.ts`** — инициализация бота, команды, обработка сообщений
- **`src/flows/`** — пошаговые сценарии диалогов (утреннее заполнение, вечерний отчет, редактирование)
- **`src/services/`** — бизнес-логика (расчеты, PDF-генерация)
- **`src/db/`** — обертки над Vercel KV для работы с данными
- **`src/admin/`** — логика админ-панели
- **`src/types/`** — типы и интерфейсы сущностей
- **`api/webhook.ts`** — Vercel Function для обработки вебхука

## 📊 Модели данных

### User (Пользователь)
- `id` — уникальный идентификатор
- `telegram_id` — ID пользователя в Telegram
- `username` — имя пользователя
- `phone` — номер телефона
- `role` — роль: `user`, `admin`, `superadmin`
- `created_at` — дата создания

### Session (Сессия)
- `id` — идентификатор сессии
- `user_id` — ID пользователя
- `state` — текущее состояние диалога
- `context` — контекст заполнения (JSON)
- `updated_at` — время последнего обновления

### Site (Площадка)
- `id` — уникальный идентификатор
- `name` — название площадки
- `responsible_user_id` — ID ответственного
- `bonus_target` — бонусная планка (в копейках)
- `phone` — телефон ответственной
- `date` — дата (YYYY-MM-DD)
- `status` — статус: `morning_filled`, `evening_filled`, `completed`
- `created_at`, `updated_at` — временные метки

### DailyReport (Отчет за день)
- `id` — уникальный идентификатор
- `site_id` — ID площадки
- `date` — дата отчета
- `lastname`, `firstname` — ФИО сотрудника
- `qr_number` — номер QR
- `qr_amount`, `cash_amount`, `terminal_amount` — суммы (в копейках)
- `comment` — комментарий
- `signature`, `responsible_signature` — подписи
- Рассчитанные поля: `total_revenue`, `salary`, `responsible_salary`, `total_daily`, `total_cash`, `total_qr`, `cash_in_envelope`
- `pdf_url` — ссылка на PDF (опционально)
- `created_at`, `updated_at` — временные метки

### Log (Лог действий)
- `id` — идентификатор
- `user_id` — ID пользователя
- `action_type` — тип действия
- `payload_before`, `payload_after` — данные до/после изменения
- `timestamp` — время действия

## 🔄 Потоки диалогов

### Утреннее заполнение

1. Пользователь нажимает "🌅 Заполнить площадку (утро)"
2. Бот запрашивает название площадки
3. Бот запрашивает бонусную планку
4. Бот запрашивает номер телефона ответственной
5. Создается запись площадки со статусом `morning_filled`

### Вечерний отчет

1. Пользователь нажимает "🌆 Заполнить площадку (вечер)"
2. Если несколько площадок — выбор площадки
3. Последовательно запрашиваются:
   - Фамилия
   - Имя
   - № QR
   - Сумма по QR
   - Сумма наличных
   - Сумма по терминалу (необязательно)
   - Комментарий (необязательно)
   - Подпись
   - Подпись ответственного
4. Автоматический расчет всех показателей
5. Сохранение отчета и обновление статуса площадки

### Редактирование

**По фамилии:**
1. Выбор режима "По фамилии"
2. Ввод фамилии
3. Выбор отчета (если несколько)
4. Последовательное редактирование полей с возможностью пропуска

**По площадке:**
1. Выбор режима "По площадке"
2. Выбор площадки и даты
3. Выбор отчета (если несколько)
4. Последовательное редактирование полей

## 🔐 Админ-панель

Доступна пользователям с ролью `admin` или `superadmin`.

**Функции:**
- Просмотр площадок за день
- Генерация и получение PDF-отчетов
- Добавление админов (только для `superadmin`)

## 🚀 Запуск локально

### Требования

- Node.js 18+
- npm или yarn
- Telegram Bot Token
- Vercel KV (для хранения данных)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Aquagrim
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
BOT_TOKEN=your_telegram_bot_token_here
KV_REST_API_URL=your_vercel_kv_rest_api_url
KV_REST_API_TOKEN=your_vercel_kv_rest_api_token
SUPERADMIN_IDS=123456789,987654321
PORT=3000
```

4. Соберите проект:
```bash
npm run build
```

5. Запустите бота в режиме polling:
```bash
npm run dev
```

## 📦 Деплой на Vercel

### Подготовка

1. Создайте проект на Vercel
2. Подключите Vercel KV в настройках проекта
3. Добавьте переменные окружения в Vercel:
   - `BOT_TOKEN`
   - `KV_REST_API_URL`
   - `KV_REST_API_TOKEN`
   - `SUPERADMIN_IDS`

### Деплой

```bash
vercel --prod
```

### Настройка вебхука

После деплоя настройте вебхук Telegram:

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-project.vercel.app/api/webhook"
```

## 📝 Команды и клавиатуры

### Основные команды

- `/start` — запуск бота, показ главного меню
- `/help` — справка по использованию

### Клавиатура главного меню

- 🌅 Заполнить площадку (утро)
- 🌆 Заполнить площадку (вечер)
- ✏️ Редактировать данные
- ℹ️ Помощь

### Клавиатура в процессе заполнения

- ⏭️ Далее — пропустить текущий шаг (для необязательных полей)
- ⬅️ Назад — вернуться на предыдущий шаг
- ❌ Отмена — отменить заполнение (с подтверждением)

## 🧮 Автоматический расчет

После ввода всех сумм бот автоматически рассчитывает:

- **Выручка** = сумма по QR + наличные + терминал
- **Зарплата** = 20% от выручки
- **Зарплата ответственного** = зарплата
- **Оборот за день** = выручка
- **Общая сумма наличных** = сумма наличных
- **Общая сумма по QR** = сумма по QR
- **Нал в конверте** = наличные - бонусы/штрафы

## 📄 PDF-отчеты

PDF генерируется с использованием `pdfkit` и включает:

- Информацию о площадке и дате
- Данные сотрудника
- Все финансовые показатели
- Комментарии и подписи

Админы могут запросить PDF через админ-панель.

## ✅ Валидация

- **Суммы**: принимаются только числа (можно с десятичными, запятая или точка)
- **Обязательные поля**: проверяются перед переходом к следующему шагу
- **Повторный запрос**: при ошибке валидации бот просит ввести значение заново

## 📊 Логирование

Все действия логируются:

- Создание пользователя
- Начало/завершение заполнения
- Редактирование полей (старое → новое значение)
- Добавление/удаление админов
- Генерация PDF

## 🔒 Безопасность

- HTTPS для вебхука (обеспечивается Vercel)
- Роли и права доступа
- Валидация всех входных данных
- Минимизация хранения персональных данных

## 🛠️ Технологии

- **TypeScript** — типизированный JavaScript
- **Telegraf** — фреймворк для Telegram ботов
- **Vercel KV** — хранилище данных
- **Vercel Functions** — серверные функции
- **pdfkit** — генерация PDF

## 📁 Структура проекта

```
Aquagrim/
├── api/
│   └── webhook.ts          # Vercel Function для вебхука
├── src/
│   ├── bot.ts              # Основной файл бота
│   ├── index.ts            # Точка входа для локальной разработки
│   ├── config.ts           # Конфигурация
│   ├── types/
│   │   └── index.ts        # Типы и интерфейсы
│   ├── db/
│   │   ├── kv.ts           # Обертки над Vercel KV
│   │   └── index.ts
│   ├── services/
│   │   ├── CalculationService.ts  # Расчеты
│   │   ├── PDFService.ts          # Генерация PDF
│   │   └── index.ts
│   ├── flows/
│   │   ├── morningFill.ts         # Утреннее заполнение
│   │   ├── eveningReport.ts       # Вечерний отчет
│   │   ├── editFlow.ts            # Редактирование
│   │   └── index.ts
│   └── admin/
│       ├── adminPanel.ts          # Админ-панель
│       └── index.ts
├── package.json
├── tsconfig.json
├── vercel.json
└── README.md
```

## 🐛 Отладка

Для локальной отладки используйте:

```bash
npm run dev
```

Логи выводятся в консоль. Для продакшена логи доступны в Vercel Dashboard.

## 📞 Поддержка

При возникновении проблем:

1. Проверьте переменные окружения
2. Убедитесь, что Vercel KV подключен
3. Проверьте логи в Vercel Dashboard
4. Убедитесь, что вебхук настроен правильно

## 📄 Лицензия

MIT

Конец.

